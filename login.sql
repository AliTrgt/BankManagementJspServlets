CREATE TABLE login
  (
    id Serial,
    name varchar(50),
    surname varchar(50),
    username varchar(50) NOT NULL,
    password varchar(50) NOT NULL,
    email varchar(50),
    int balance
  
  );
